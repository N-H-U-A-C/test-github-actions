name: premier workflow

on: workflow_dispatch

jobs:
  premier-job:
    runs-on: ubuntu-latest
    steps:
      - name: bonjour
        run: echo "hello world!"
      - name: au revoir
        run: echo "goodbye!"